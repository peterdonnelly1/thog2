#!/bin/bash
set -euo pipefail

# vvv THOG
# Internal dreedle implementation delegate for current_dreedle_train_OWT.sh.
# Optimizer selection has already been resolved by optimizer_train_OWT_wrapper.sh:
#   -y / --optimizer: adamw | sgd | sgd_nesterov | adafactor | rmsprop
#   --optimizer-momentum: momentum for SGD, Nesterov SGD, and RMSprop
# The resolved optimizer is exported as THOG2_OPTIMIZER; optimizer-specific -c/-f
# defaults have already been inserted unless the caller supplied explicit values.
# ^^^ THOG

source ./.current_dreedle_train_OWT_body
