name: Apply PLASTIC CLI console refinements

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired_writeback:
    runs-on: ubuntu-latest
    steps:
      - name: Historical apply workflow is retired
        run: echo 'Superseded by the validated PLASTIC v0.52 migration workflow.'

# vvv THOG retire the legacy non-idempotent writeback while preserving its manual historical workflow entry
# ^^^ THOG
