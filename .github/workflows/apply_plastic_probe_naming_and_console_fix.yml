name: Apply PLASTIC probe naming and console fix

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired_writeback:
    runs-on: ubuntu-latest
    steps:
      - name: Historical apply workflow is retired
        run: echo 'Superseded by the validated PLASTIC v0.52 migration workflow.'

# vvv THOG retire the superseded probe-name writeback so it cannot restore retired public controls
# ^^^ THOG
