name: Temporary PREMAT forensic tranche

on:
  push:
    branches:
      - THOG2_Dynamic_Prematerialisation_Enhancement
    paths:
      - .github/workflows/tmp_premat_forensic_tranche.yml
      - tmp_apply_premat_forensic_tranche.py
      - tmp_refine_premat_forensic_tranche.py
      - tmp_fix_premat_forensic_regressions.py

permissions:
  contents: write

jobs:
  apply_and_verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: THOG2_Dynamic_Prematerialisation_Enhancement
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install test dependencies
        run: python -m pip install -q pytest torch matplotlib plotly
      - name: Apply forensic tranche
        run: |
          python tmp_apply_premat_forensic_tranche.py
          python tmp_refine_premat_forensic_tranche.py
          python tmp_fix_premat_forensic_regressions.py
      - name: Verify
        run: |
          python -m compileall -q sheet tests/test_premat.py
          git diff --check
          python -m pytest -q tests/test_premat.py tests/test_local_chart_dashboard.py tests/test_instra_live_loss.py
      - name: Commit implementation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add sheet/premat.py sheet/model.py sheet/local_dashboard_assets/dashboard_premat.js tests/test_premat.py
          git commit -m 'Add PREMAT first-tranche GPU forensics'
          git push origin HEAD:THOG2_Dynamic_Prematerialisation_Enhancement
